// nomeie 3 propriedades ou métodos de strings

var nome = 'Raíza'; 

nome.length // Descobre o comprimento de uma string
nome.toLowerCase // converte os caracteres para minusculo
nome.toUpperCase // converte os caracteres para maiúsculo
nome = nome.replace('Ra', 'Ta') // substitui uma substring dentro de uma string com uma outra substring


// nomeie 5 propriedades ou métodos de elementos do DOM

var btn = document.querySelector('.btn')
  //Propriedades

  // documentElement -> Retorna o elemento que é filho direto do documento. 
  // hidden -> propriedade read-only retorna um valor booleano indicando se a página é considerada oculta ou não.

  // Métodos

  // createElement -> Cria um novo elemento com o nome de tag fornecido.
  // getElementsByClassName 


// busque na web um objeto (método) capaz de interagir com o clipboard, 
// clipboard é a parte do seu computador que lida com o CTRL + C e CTRL + V
